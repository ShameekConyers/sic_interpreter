#pragma once

#include "scanner.hpp"
// #include "chunk.hpp"

void compile(const std::string& source)
{
  Scanner scanner{ source };

  int line = -1;
  // while (true) {
  //   Token token = scanner.scan_token();
  //   if (token.m_line != line != line)
  // }
}
